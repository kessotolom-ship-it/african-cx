# ===========================================
#  African-CX — Variables d'environnement
# ===========================================

# Database Connection (Required)
# Use a cloud provider like Neon, Supabase, or Vercel Postgres
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# Mastra Configuration
NODE_ENV=development
MASTRA_APP_URL=http://localhost:3000

# OpenAI (Required — pour les agents et embeddings)
OPENAI_API_KEY=sk-...

# RAG — PGVector Database (peut être le même que DATABASE_URL)
POSTGRES_URL="postgresql://user:password@host:port/database?sslmode=require"

# ===========================================
#  Evolution API (WhatsApp Integration)
# ===========================================

# URL de base de votre instance Evolution API
# Ex: https://evo.votredomaine.com ou http://localhost:8080
EVOLUTION_API_URL=

# Clé API globale d'Evolution API (configurée au deploy)
EVOLUTION_API_KEY=

# Nom de l'instance WhatsApp dans Evolution API
# Créée via le panneau admin d'Evolution API
EVOLUTION_INSTANCE=solimi

# Secret pour valider les webhooks entrants (optionnel en dev)
# Si vide, tous les webhooks sont acceptés (DEV MODE)
EVOLUTION_WEBHOOK_SECRET=
